<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:cardview="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cast_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:foreground="?attr/selectableItemBackground"
    cardview:cardBackgroundColor="@color/cardBackground"
    cardview:cardPreventCornerOverlap="false"
    cardview:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/dist_medium"
        android:orientation="vertical">

        <!-- Cast image -->
        <ImageView
            android:id="@+id/cast_image"
            android:layout_width="@dimen/movie_list_img_size"
            android:layout_height="@dimen/movie_list_img_size"
            android:background="@color/cardview_shadow_start_color" />

        <!-- Text Area -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Name -->
            <TextView
                android:id="@+id/cast_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAlignment="center"
                tools:text="Bruce Wayne" />

            <!-- Role -->
            <TextView
                android:id="@+id/cast_role"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAlignment="center"
                tools:text="as Batman" />

        </LinearLayout>

    </LinearLayout>

</android.support.v7.widget.CardView>